{
  "name": "ksef-pdf-generator",
  "version": "1.6.0",
  "type": "module",
  "description": "CLI tool for generating PDF visualizations of KSeF invoices and UPO documents from XML files",
  "private": true,
  "bin": {
    "ksef-pdf-generator": "./dist/cli.cjs"
  },
  "scripts": {
    "build": "npm run bundle-cli && npm run bundle-server && npm run copy-worker",
    "bundle-cli": "esbuild src/cli/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/cli.cjs --external:fsevents --minify --tree-shaking=true --keep-names",
    "bundle-server": "esbuild src/server/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/server.cjs --external:fsevents --minify --tree-shaking=true --keep-names",
    "copy-worker": "node -e \"require('fs').copyFileSync('node_modules/jsdom/lib/jsdom/living/xhr/xhr-sync-worker.js', 'dist/xhr-sync-worker.js')\"",
    "cli": "node dist/cli.cjs",
    "server": "node dist/server.cjs",
    "setup": "npm install && npm run build",
    "clean": "rimraf node_modules dist build .idea .vscode",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "node tests/run-all.mjs",
    "analyze-bundle": "node scripts/analyze-bundle.mjs"
  },
  "keywords": [
    "ksef",
    "pdf",
    "invoice",
    "upo",
    "generator",
    "cli"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jsdom": "^27.0.0",
    "@types/node": "^22.19.0",
    "@types/pdfmake": "^0.3.3",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "esbuild": "^0.27.0",
    "rcedit": "^5.0.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "@types/multer": "^2.0.0",
    "express": "^5.2.1",
    "i18next": "^25.8.4",
    "jsdom": "^26.1.0",
    "multer": "^2.0.2",
    "pdf-lib": "^1.17.1",
    "pdfmake": "0.3.7",
    "xml-js": "^1.6.11"
  }
}
